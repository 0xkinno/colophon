{
  "name": "colophon-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm --recursive --parallel run test",
    "typecheck": "pnpm --recursive run typecheck",
    "build": "pnpm --recursive run build",
    "verify": "node scripts/verify-bundle.mjs",
    "benchmark": "node scripts/e4-baseline.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
